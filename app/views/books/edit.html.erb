<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>body</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<%= link_to "edit", edit_book_path(@book.id) %>
<%= link_to "back", { :action => "index" }, :method => "get" %>
<% end %>